services:
    krista-app:
        build:
            context: .
            dockerfile: Dockerfile
        networks:
            - internal
        restart: always
        ports:
            - "1234:1234"

networks:
    internal:
        external: true
        driver: bridge